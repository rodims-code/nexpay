import { createFileRoute } from '@tanstack/react-router'
import { Download, Search } from 'lucide-react'
import { DashboardLayout } from '#/components/dashboard/dashboard-layout'
import { demoTransactions } from '#/components/dashboard/dashboard-data'
export const Route = createFileRoute('/dashboard/transactions')({ component: TransactionsPage })
function TransactionsPage() { return <DashboardLayout title="Transactions"><div className="space-y-5"><div className="flex flex-col gap-3 sm:flex-row"><label className="input input-bordered flex flex-1 items-center gap-2 rounded-2xl"><Search className="size-4 text-base-content/40" /><input placeholder="Rechercher une transaction" /></label><button className="btn btn-outline rounded-2xl"><Download className="size-4" /> Exporter</button></div><div className="overflow-x-auto rounded-[1.75rem] border border-base-200 bg-base-100"><table className="table"><thead><tr><th>Destinataire</th><th>Référence</th><th>Date</th><th>Montant</th><th>Statut</th></tr></thead><tbody>{demoTransactions.map((item) => <tr key={item.id} className="hover"><td><div className="flex items-center gap-3"><div className={`flex size-9 items-center justify-center rounded-xl text-xs font-bold ${item.tone}`}>{item.initials}</div><span className="font-bold">{item.name}</span></div></td><td className="text-xs text-base-content/50">{item.id}</td><td className="text-sm text-base-content/60">{item.date}</td><td className="font-bold">{item.amount}</td><td><span className={`badge badge-sm rounded-full border-0 font-bold ${item.tone}`}>{item.status}</span></td></tr>)}</tbody></table></div></div></DashboardLayout> }
