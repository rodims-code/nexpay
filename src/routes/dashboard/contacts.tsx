import { createFileRoute } from '@tanstack/react-router'
import { Phone, Plus, Search, Star } from 'lucide-react'
import { DashboardLayout } from '#/components/dashboard/dashboard-layout'
import { demoContacts } from '#/components/dashboard/dashboard-data'
export const Route = createFileRoute('/dashboard/contacts')({ component: ContactsPage })
function ContactsPage() { return <DashboardLayout title="Mes contacts"><div className="space-y-5"><div className="flex flex-col justify-between gap-3 sm:flex-row"><label className="input input-bordered flex items-center gap-2 rounded-2xl"><Search className="size-4 text-base-content/40" /><input placeholder="Rechercher un contact" /></label><button className="btn btn-primary rounded-2xl"><Plus className="size-4" /> Nouveau contact</button></div><div className="grid gap-4 md:grid-cols-2">{demoContacts.map((contact) => <div key={contact.phone} className="card rounded-3xl border border-base-200 bg-base-100"><div className="card-body flex-row items-center gap-4 p-5"><div className={`flex size-12 shrink-0 items-center justify-center rounded-2xl text-sm font-extrabold ${contact.color}`}>{contact.initials}</div><div className="min-w-0 flex-1"><h3 className="truncate font-bold">{contact.name}</h3><p className="flex items-center gap-1 text-sm text-base-content/50"><Phone className="size-3" /> {contact.phone}</p></div>{contact.favorite && <Star className="size-4 fill-warning text-warning" />}<button className="btn btn-ghost btn-sm rounded-full">Envoyer</button></div></div>)}</div></div></DashboardLayout> }
